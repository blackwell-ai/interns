# inbox — file findings as inbox/queue/ tasks

No connection needed. Bridges flows into the repo task protocol (inbox/TEMPLATE.md shape).

## inbox.file
- in: JSONL records (one task each)
- flags: `--title-field summary`, `--assign-to human`, `--source <who created these>`
