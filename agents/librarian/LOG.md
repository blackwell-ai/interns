# Librarian log

Newest first. One entry per nightly run, trimmed to the last 30. Each entry says
what was deleted, what was moved, what was flagged for a human, and what was left
alone. The run is the `librarian-nightly` skill; the charter is `AGENT.md`.

## 2026-06-22, setup

Agent chartered and the nightly skill created. No cleanup pass has run yet; the
first real entry lands the night the timer first fires.
