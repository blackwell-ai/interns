# Recon: https://goodmolecules.com   (2026-07-01T00:30Z)
## Homepage
platform shopify: 0
title: <title>See a difference in your skin | Good Molecules</title>
(empty title above = no server-rendered <title>, itself a discoverability finding) 
meta desc: <meta name="description" content="Shop skincare with science-backed ingredients for all skin types. Target hyperpigmentation, acne, texture and pores, dry skin, fine lines, and more with vegan, cruelty-free formulas starting at just $6."
h1 count: 0
og:image: homepage ld+json @types:    1 "@type": "ContactPoint"    1 "@type": "EntryPoint"    1 "@type": "OnlineStore"    1 "@type": "OpeningHoursSpecification"    1 "@type": "SearchAction"    1 "@type": "WebSite" 
pixels: g-f79bbf9a 
meta system-user token (empty/dash = CAPI not wired): 
## AI / agent layer
robots references: 
## Catalog size (sitemaps)
products total: 0
collections: n/a
blog article URLs: 0
## Product page schema
(no product handle; pass one as arg 2 for non-Shopify sites)
## Agentic commerce layer (HTTP status per surface)
  /agents.md -> HTTP 200
  /llms-full.txt -> HTTP 202
  /.well-known/ucp -> HTTP 202
  /api/ucp/mcp -> HTTP 202
  /sitemap_agentic_discovery.xml -> HTTP 202
## Crawlability: AI bot user-agents (HTTP status on a product page)
  GPTBot -> HTTP 200
  OAI-SearchBot -> HTTP 200
  ClaudeBot -> HTTP 200
  PerplexityBot -> HTTP 200
  Google-Extended -> HTTP 202
  Amazonbot -> HTTP 405
  Bingbot -> HTTP 405
## Limitation: storefront serves 202 (WAF challenge) to curl (2026-06-30)

Direct curl to the hero PDP paths returns HTTP 202, not 200, for both generic
browser and bot user-agents:

  202  https://www.goodmolecules.com/s/good-molecules-discoloration-correcting-serum-30ml
  202  https://www.goodmolecules.com/s/good-molecules-discoloration-correcting-serum
  202  https://goodmolecules.com/s/good-molecules-niacinamide-serum-30ml
  405  https://www.goodmolecules.com/products/discoloration-correcting-serum  (legacy path)

202 is a WAF/CDN interstitial for non-JS clients, so PDP JSON-LD (Product / Offer /
AggregateRating) CANNOT be confirmed via curl this run. The bot user-agents scored
200 on the product page probed by recon.sh above (declared AI bots are allowlisted),
but the storefront challenges generic clients. Consequence: the Quotability truth
table (server-side PDP schema depth) must be read in the headed browser during the
battery, viewing page source after JS/challenge resolves. Same caveat applies to the
202 agentic endpoints (llms-full.txt, .well-known/ucp, api/ucp/mcp) — 202 is not a
confirmed live handler; resolve in-browser. Only /agents.md (200) is confirmed served.

# End recon. Save this whole output to agents/geo/<client>/recon-<YYYY-MM-DD>.md
